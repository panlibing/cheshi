# ============================================================================
# cheshi 仓库忽略规则
#   适用范围: 整个仓库; 子目录可通过同名 .gitignore 追加规则
#   注意: .gitignore 只对「未跟踪」文件生效; 已被 git 跟踪的文件需先
#         git rm --cached <path> 出库, 之后规则才会起作用
# ============================================================================

# ---- 构建产物: Windows / MSVC ----
*.exe
*.obj
*.pdb
*.ilk
*.exp
*.lib
*.dll

# ---- 构建产物: Linux / macOS ----
*.o
*.a
*.so
*.so.*
*.dylib
# 与目录同名的可执行文件(用斜杠限定, 避免误伤同名目录/文件)
/port_relay/portrelay
/port_relay/tests/echo
/port_relay/tests/probe

# ---- IDE / 编辑器 / 系统临时文件 ----
.vs/
.vscode/
.idea/
*.user
*.suo
*.swp
*~
Thumbs.db
Desktop.ini
.DS_Store

# ---- 运行时本地文件 ----
# 不带参数启动时自动读取的配置文件属于「本机配置」, 一般不入库;
# 确实需要入库时用 git add -f 强制添加
portrelay.conf
/port_relay/portrelay.conf
/port_relay/tests/*.conf
*.log

# ---- 本机调试脚手架 / 探针脚本 / 临时日志(仅本地使用, 不入库) ----
/_localdev/
